enum SectionType { text, image, header, table, formula }
enum HeadingStyle { h1, h2, h3, body, caption }
enum DocumentStructure { hierarchical, linear, matrix }
enum AccessLevel { view, comment, edit, admin }